# Copy this file to .env and replace the placeholders with local values.
# Configure your IDE, shell, or container runtime to load the file.
DB_PASSWORD=replace-with-your-postgres-password

# Base64-encoded key with at least 32 random bytes for HS256.
# Example generator: openssl rand -base64 32
JWT_SECRET=replace-with-a-new-base64-secret
